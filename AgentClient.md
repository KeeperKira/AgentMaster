# AgentClient — TODO List для разработчика

> **Версия:** 16 апреля 2026  
> **Проект:** AgentMaster → AgentClient

---

## 1. Парсинг и валидация JSON

- [ ] Парсинг JSON-файла схемы агента
- [ ] Валидация структуры (`nodes`, `connections`)
- [ ] Проверка уникальности ID узлов
- [ ] Валидация типов узлов (из допустимого списка)
- [ ] Проверка существования узлов в связях
- [ ] Обработка ошибок с понятными сообщениями

---

## 2. Базовая архитектура

- [ ] Класс `Node` — хранение данных узла
- [ ] Класс `Connection` — хранение связи
- [ ] Класс `AgentSchema` — коллекция узлов и связей
- [ ] Словарь `nodesById` для быстрого доступа
- [ ] Граф зависимостей (reverse adjacency list)

---

## 3. Выполнение агента

- [ ] Топологическая сортировка узлов
- [ ] Обнаружение циклов в графе
- [ ] Выполнение узлов в правильном порядке
- [ ] Передача данных между узлами
- [ ] Обработка ошибок выполнения

---

## 4. Реализация узлов

### Базовые узлы

- [ ] **Input** — получение входных данных от пользователя
- [ ] **Output** — формирование финального ответа

### Текстовые узлы

- [ ] **Text** — возврат многострочного текста
- [ ] **Triplet** — сбор данных в структуру `{subject, predicate, object}`

### Обработка данных

- [ ] **Concat** — объединение потоков (с флагом `wait`)
- [ ] **Router** — HTTP-запрос к API с `Authorization: Bearer`
- [ ] **Logger** — запись данных в файл

### Условная логика

- [ ] **Gate** — пропуск/блокировка по условию
- [ ] **Summa** — сумматор с условием (специфично)

---

## 5. Типы данных и интерфейсы

- [ ] Интерфейс `INodeExecutor` — метод `Execute(inputs: List<string>)`
- [ ] Базовый класс `NodeExecutor` с通用ей реализацией
- [ ] Тип `DataPacket` — структура передаваемых данных
- [ ] Поддержка JSON в `DataPacket`

---

## 6. HTTP и API

- [ ] HTTP-клиент для Router
- [ ] Поддержка заголовков (`Authorization`, `Content-Type`)
- [ ] Обработка ошибок HTTP (timeout, status codes)
- [ ] JSON сериализация/десериализация запросов/ответов

---

## 7. Конфигурация

- [ ] Загрузка конфига из файла (API ключи, настройки)
- [ ] Переменные окружения для чувствительных данных
- [ ] Кэширование результатов (опционально)

---

## 8. Логирование и отладка

- [ ] Логирование выполнения узлов
- [ ] Логирование ошибок
- [ ] Подробный режим (`--verbose`)
- [ ] Вывод метрик (время выполнения каждого узла)

---

## 9. CLI и запуск

- [ ] Точка входа `agent-client.exe`
- [ ] Аргументы командной строки:
  - [ ] `--schema <path>` — путь к JSON схеме
  - [ ] `--input <text>` — входные данные
  - [ ] `--config <path>` — путь к конфигу
  - [ ] `--verbose` — подробный режим
- [ ] Вывод результата в stdout

---

## 10. Тестирование

- [ ] Юнит-тесты для каждого типа узлов
- [ ] Интеграционные тесты полных схем
- [ ] Тесты обработки ошибок
- [ ] Тесты циклов в графе

---

## 11. Документация

- [ ] README.md с инструкциями по сборке
- [ ] Примеры JSON схем
- [ ] Примеры выполнения CLI
- [ ] API документация для разработчиков

---

## 12. Будущие улучшения

- [ ] Потоковая передача данных (streaming)
- [ ] Параллельное выполнение независимых узлов
- [ ] Восстановление после сбоев (checkpointing)
- [ ] Интерактивный режим с REPL
- [ ] Веб-интерфейс для отладки

---

## Текущий приоритет

**Фаза 1 ( MVP ):**
1. Парсинг JSON
2. Input, Output, Text, Router
3. Топологическая сортировка
4. CLI с базовым запуском

**Фаза 2 ( Расширение ):**
5. Concat, Triplet, Logger
6. Gate, Summa
7. Конфигурация и логирование

**Фаза 3 ( Полноценность ):**
8. Оптимизации и потоки
9. Тестирование
10. Документация
